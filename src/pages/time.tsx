import React from 'react';
import { PageTemplate } from "../components/page-template";

export const Time: React.FC<unknown> = () => {
    return (
        <PageTemplate>
            Время
        </PageTemplate>
    );
}